# Adaptive UI design system (Mox demo capture)

> Living capture of adaptive UI design decisions as they are made in this repo. The planner pulls the "For pull-back to canon" section into the doc set at `24_adaptive_ui/design_system.md` at session boundaries, so this visual work compounds across every product surface instead of staying in the demo. The canonical vision is doc_repo `24_adaptive_ui/adaptive_ui_vision.md` (a copy lives at `docs/adaptive_ui_vision.md` in this repo).

## How to use this doc (Chris's agent)

As you make design decisions (component aesthetics, the confidence / citation / timestamp treatment, the adaptive command choreography, motion), record them here in two parts:

- **Working notes** — anything in progress.
- **For pull-back to canon** — settled decisions that should become portfolio canon. The planner reads this section.

Honor the hard constraints in `chris_coordination_memo.md` and the six honesty guardrails in `README.md`.

## For pull-back to canon

(empty — fill as decisions settle)

## Working notes

(empty — fill as you work)
