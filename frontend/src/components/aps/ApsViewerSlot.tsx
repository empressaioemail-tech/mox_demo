"use client";

/**
 * APS viewer slot — WS-4 wires @aps_sdk/autodesk-sdk and the model URN from WS-1.
 * Placeholder until APS credentials and URN are available.
 */
export function ApsViewerSlot() {
  return (
    <div
      className="flex h-48 w-full items-center justify-center rounded-lg border border-dashed border-zinc-300 bg-zinc-50 text-sm text-zinc-500"
      data-slot="aps-viewer"
    >
      APS viewer slot (WS-4)
    </div>
  );
}
