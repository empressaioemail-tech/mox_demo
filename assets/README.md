# Heavy assets (gitignored)

Large files live under `apartment_bldg/` at the repo root (gitignored). Do not commit them.

Source RVT:

```
apartment_bldg/Apartments-20260613T130315Z-3-001/Apartments/Working Files/5 Story Apartment.rvt
```

Renderings, elevations, floor plans, schedules, and structural PDF are in sibling folders under the same `Apartments/` tree. Full inventory: `docs/property_ground_truth.md`.

WS-1 uploads the RVT to APS and records the model URN here as `aps-model.json` (pointer only, not the binary).
